[project]
name = "profiles-rest-api"
version = "0.1.0"
description = "Basic profiles api project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django==2.2",
    "djangorestframework>=3.9.1",
    "legacy-cgi>=2.6.3",
    "setuptools",
    "uwsgi>=2.0.18",
]
